# Cloudflare Pages Redirects
# Note: Root-to-WWW redirects should be configured in the Cloudflare Dashboard 
# under 'Domain Redirection' or 'Page Rules' for maximum SEO reliability.

# SPA Fallback (only if using dynamic client-side routes)
# /fallback  /index.html  200
